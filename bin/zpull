#!/usr/bin/env bash

pushd ~/z/hg > /dev/null
for dir in $(find . -maxdepth 1 -mindepth 1 -type d)
	do
		echo
		echo pulling $dir...
		pushd $dir > /dev/null
		hg pull
		hg update
		popd > /dev/null
	done
popd > /dev/null
