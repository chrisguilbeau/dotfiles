#!/usr/bin/env bash
if [ $# -ne 2 ]
  then
    echo "usage: $0 level name"
    exit 1
fi

set -e
src=~/z/hg/$1
dest=~/z/proj/$1_$2

mkdir $dest
pushd $src && zhg pull -u && popd
cp -R $src/* $dest
echo $1 > $dest/meta.level
echo $2 > $dest/meta.name
touch $dest/meta.time
zswitch $dest
z?
